# Feature Components

Place feature-oriented components here once domain features exist.

- Keep `ui/` components generic and reusable.
- Move feature-specific behavior and composition into this folder.
